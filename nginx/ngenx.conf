worker_processes 1;

user nobody nogroup;
# 'user nobody nobody;' for systems with 'nobody' as a group instead
error_log  /app/log/error.log warn;
pid /var/run/nginx.pid;

events {
  worker_connections 64; # increase if you have lots of clients
  accept_mutex off; # set to 'on' if nginx worker_processes > 1
  use epoll; # to enable for Linux 2.6+
  # 'use kqueue;' to enable for FreeBSD, OSX
}

http {
  include mime.types;
  # fallback in case we can't determine a type
  default_type application/octet-stream;
  access_log /app/log/fallback.log combined;
  sendfile on;

  # upstream app_server {
  #   # fail_timeout=0 means we always retry an upstream even if it failed
  #   # to return a good HTTP response

  #   # for UNIX domain socket setups
  #   # server unix:/tmp/gunicorn.sock fail_timeout=0;

  #   # for a TCP configuration
  #   # server 192.168.0.7:8000 fail_timeout=0;
  # }

  server {
    # if no Host match, close the connection to prevent host spoofing
    listen 80 default_server;
    return 444;
  }

  server {
    # use 'listen 80 deferred;' for Linux
    listen 80 deferred;
    client_max_body_size 1M;

    # set the correct host(s) for your site
   server_name www.dylan-shah.com dev.dylan-shah.com localhost;

   return 302 https://blahaj.ca/;
  }
}